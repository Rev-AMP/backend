image: tiangolo/docker-with-compose

before_script:
  - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $CI_REGISTRY
  - pip install docker-auto-labels

stages:
  - test
  - build
  - deploy

tests:
  stage: test
  script:
    - sh ./scripts/test.sh
  tags:
    - build
    - test

build-stag:
  stage: build
  script:
    - TAG=stag FRONTEND_ENV=staging sh ./scripts/build-push.sh
  only:
    - master
  tags:
    - build
    - test
    
build-prod:
  stage: build
  script:
    - TAG=prod FRONTEND_ENV=production sh ./scripts/build-push.sh
  only:
    - production
  tags:
    - build
    - test

deploy-stag:
  stage: deploy
  script: 
    - >
      DOMAIN=stag.epic-erp.akhilnarang.dev
      TRAEFIK_TAG=stag.epic-erp.akhilnarang.dev
      STACK_NAME=stag-epic-erp-akhilnarang-dev
      TAG=stag
      sh ./scripts/deploy.sh
  environment:
    name: staging
    url: https://stag.epic-erp.akhilnarang.dev
  only:
    - master
  tags:
    - swarm
    - stag

deploy-prod:
  stage: deploy
  script:
    - >
      DOMAIN=epic-erp.akhilnarang.dev
      TRAEFIK_TAG=epic-erp.akhilnarang.dev
      STACK_NAME=epic-erp-akhilnarang-dev
      TAG=prod
      sh ./scripts/deploy.sh
  environment:
    name: production
    url: https://epic-erp.akhilnarang.dev
  only:
    - production
  tags:
    - swarm
    - prod
